{
  "mcpServers": {
    "muv": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/bin/muv.js", "--mcp"],
      "env": {
        "MUV_CONFIG_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
